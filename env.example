# =============================================================================
# SOCIAL CONNECT - ENVIRONMENT VARIABLES
# =============================================================================
# This file contains only the environment variables that are actually used 
# in the project codebase based on the gateway controllers.

# -----------------------------------------------------------------------------
# API CONFIGURATION
# -----------------------------------------------------------------------------

# Frontend API Base URL - Used by the frontend to communicate with backend gateway
# Required for: Authentication, X (Twitter) API, Instagram API
# Used in: src/lib/api.ts, src/components/debug/, src/lib/utils/ngrok-helper.ts
# 
# VERCEL DEPLOYMENT: Use proxy path (Vercel handles rewrites to NGROK)
# NEXT_PUBLIC_API_URL=/api/v1
# 
# FOR DIRECT NGROK ACCESS: Uncomment below (bypasses proxy)
# NEXT_PUBLIC_API_URL=https://e305103aab93.ngrok-free.app/api/v1
# 
# FOR AUTO-DETECTION: Leave empty (recommended)
NEXT_PUBLIC_API_URL=

# Backend API URL - Used by API routes to communicate with backend gateway
# Used in: api-backup/v1/x/analytics/user/route.ts, api-backup/v1/x/analytics/tweets/route.ts
# For NGROK: Use the direct NGROK URL for server-side requests
BACKEND_API_URL=https://e305103aab93.ngrok-free.app

# -----------------------------------------------------------------------------
# INSTAGRAM OAUTH CONFIGURATION
# -----------------------------------------------------------------------------

# Instagram App Client ID (Public)
INSTAGRAM_CLIENT_ID=1316102379931363

# Instagram App Client Secret (KEEP SECRET!)
# Get this from your Instagram app settings in Facebook Developer Console
INSTAGRAM_CLIENT_SECRET=your_instagram_client_secret_here

# -----------------------------------------------------------------------------
# NODE ENVIRONMENT
# -----------------------------------------------------------------------------

# Node environment - Used for development vs production logic
# Used in: src/lib/api.ts, src/lib/utils/ngrok-helper.ts, src/components/debug/
NODE_ENV=development

# -----------------------------------------------------------------------------
# OPTIONAL HEADERS (Set by frontend as needed)
# -----------------------------------------------------------------------------
# These are NOT environment variables but headers sent by the frontend:
# 
# JWT Token (Required for most endpoints):
#   Authorization: Bearer <jwt_token>
#   
# Social Media Tokens (Optional, for enhanced functionality):
#   X-Twitter-Access-Token: <twitter_oauth_token>
#   X-Instagram-Access-Token: <instagram_oauth_token>
#
# Content-Type is automatically set to application/json
